{
  "PageType": 0,
  "ColumnCount": 60,
  "RowCount": 45,
  "Formulas": {
    "23,3": "ODATA(\"YBS規格文書/$count\")",
    "23,11": "ODATA(\"YBS規格文書/$count?$filter=状態 eq '一時保存' or 状態 eq '差戻' or 状態 eq '部門長承認待ち' or 状態 eq '審議会承認待ち' or 状態 eq '委員長承認待ち'\")"
  }
}